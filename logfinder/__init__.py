"""LogsFinder — search through Lunar Client session logs.

The package is split into a pure, UI-free core (colorcodes, logs, cache, search)
and a Tkinter UI layer (app). The core imports no tkinter so it can be tested or
reused on its own.
"""

__version__ = "1.0.0"
